@model List<Employee>

<div class="display-3">

Index3
</div>

@foreach(Employee item in Model)
{
    <div class="lead my-2">
    <h6>@item.FullName</h6>
    <p>@item.Id</p>
    <strong>Yaş:@item.Age</strong>
    </div>
}
